# Deprecated location

The IndPenSim batch data has moved to `use_cases/IndPenSim/data/`.

- Test batches 91--100 are now in `use_cases/IndPenSim/data/test/`
  (with cleaned-up filenames: `batch_91.csv` ... `batch_100.csv`).
- A fetcher for the 60-batch reference set (batches 1--60) is at
  `use_cases/IndPenSim/data/download_indpensim.py`.

This folder is kept only because the files inside it are read-only and
cannot be removed from this checkout. They are functionally superseded.
